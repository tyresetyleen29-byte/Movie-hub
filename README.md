# Movie-hub
MovieHub/ ├── index.html        ← full website code (search + filters + 10/10 movies) ├── manifest.json     ← PWA support ├── sw.js             ← service worker for offline / app-like experience └── icon.png          ← app icon for home screen
